services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    # No managed vector DB credentials needed -- the local hybrid index
    # (artifacts/full_local_index) must be present on disk / mounted into
    # the container. SARVAM_API_KEY still comes from .env for STT.
