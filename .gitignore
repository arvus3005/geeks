# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
.venv-eval/
venv/
env/

# uv
.uv/

# Environment files (NEVER commit secrets)
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
.claude/
.agents/
*.swp
*.swo

# pytest
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db
__MACOSX/

# Prepared canary/pilot datasets and manifests (generated offline)
artifacts/prepared/
artifacts/checkpoints/
artifacts/dedup/
artifacts/ledgers/
artifacts/eval/
artifacts/local_index/
artifacts/full_local_index/
artifacts/full_local_index_smoke_200/
artifacts/full_index_checkpoints/
artifacts/full_index_dedup/

# Generated canary execution reports (runtime output — never committed)
artifacts/reports/canary_index_execution_*.json
artifacts/reports/canary_index_execution_*.md
artifacts/reports/chunking_ablation_*.json
artifacts/reports/chunking_ablation_*.md

# Budget ledgers
*.ledger.json

# Dedup databases
*.db

# Checkpoint files
*.checkpoint.json

# Legacy local vector-store artefacts (unused)

# Model cache / tokenizer cache
model_cache/
.cache/
hf_cache/

# Pickle / binary checkpoints
*.pkl
*.pt
*.bin

# Audio uploads
uploads/
*.wav
*.mp3
*.ogg

# Logs
*.log
logs/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Nested archives (must not be committed)
*.zip
*.tar.gz
*.tar
*.tgz

# Reference code & documentation (read-only; Git-excluded)
reference/

# Claude memory file
CLAUDE.md

# Local-only reference notes -- deliberately never committed (see git
# history: was purged from history entirely at the user's request)
docs/FOLLOWUP_TECHNIQUES.md
